import SwiftUI

@main
struct PetBnBApp: App {
    var body: some Scene {
        WindowGroup {
            // Fleshed out in Task 9 (RootView with auth gate + tabs).
            Text("PetBnB — booting…")
                .font(.headline)
        }
    }
}
